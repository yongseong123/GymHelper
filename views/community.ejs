<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Community</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <script>
      tailwind.config = {
        theme: {
          extend: {
            fontFamily: {
              sans: ["Pretendard", "Noto Sans KR", "sans-serif"]
            }
          }
        }
      };
    </script>
</head>
<body class="min-h-screen bg-gradient-to-br from-zinc-100 via-orange-50 to-cyan-50 font-sans text-zinc-800">
    <div class="main-container grid min-h-screen grid-cols-1 gap-6 p-4 lg:grid-cols-[1fr_260px] lg:p-6">
        <div class="content-wrapper order-2 lg:order-1">
            <div class="content w-full rounded-3xl border border-white/70 bg-white/90 p-6 shadow-2xl backdrop-blur">
                <div class="board-layer">
                    <h1 class="board-title mb-4 text-2xl font-semibold text-zinc-800">자유게시판</h1>
                    <div class="post-list">
                        <% if (posts && posts.length > 0) { %>
                            <div class="overflow-x-auto">
                                <table class="post-table w-full min-w-[680px] overflow-hidden rounded-2xl border border-zinc-200 text-sm">
                                    <thead class="bg-zinc-100 text-zinc-600">
                                        <tr>
                                            <th class="px-3 py-3">번호</th>
                                            <th class="px-3 py-3">제목</th>
                                            <th class="px-3 py-3">작성일</th>
                                            <th class="px-3 py-3">작성자</th>
                                        </tr>
                                    </thead>
                                    <tbody class="divide-y divide-zinc-100">
                                        <% posts.forEach(post => { %>
                                            <tr class="post-item cursor-pointer transition hover:bg-zinc-50" data-id="<%= post.board_id %>" data-title="<%= post.title %>" data-content="<%= post.content %>">
                                                <td class="px-3 py-3 text-center"><%= post.board_id %></td>
                                                <td class="px-3 py-3"><%= post.title %></td>
                                                <td class="px-3 py-3 text-center"><%= new Date(post.regdate).toISOString().slice(0, 10) %></td>
                                                <td class="px-3 py-3 text-center"><%= post.writer %></td>
                                            </tr>
                                        <% }) %>
                                    </tbody>
                                </table>
                            </div>
                        <% } else { %>
                            <p class="rounded-xl bg-zinc-100 p-4 text-sm text-zinc-500">게시글이 없습니다.</p>
                        <% } %>
                    </div>
                </div>
            </div>
        </div>

        <div class="sidebar order-1 rounded-2xl border border-zinc-800/5 bg-zinc-900/95 p-5 text-white shadow-2xl lg:order-2 lg:sticky lg:top-6 lg:h-fit">
            <a href="/work" class="logo mb-2 block text-xl font-semibold tracking-tight">GymHelper</a>
            <a href="/accountSettings" class="menu-item mb-2 block rounded-xl border border-white/20 bg-white/10 px-4 py-3 text-center text-sm font-medium transition hover:-translate-y-0.5 hover:bg-white/20">마이페이지</a>
            <a href="/community" class="menu-item mb-2 block rounded-xl border border-white/20 bg-white/10 px-4 py-3 text-center text-sm font-medium transition hover:-translate-y-0.5 hover:bg-white/20">커뮤니티</a>
            <button id="writePostBtn" class="menu-item create-post w-full rounded-xl bg-orange-500 px-4 py-3 text-sm font-semibold text-white transition hover:bg-orange-600">게시글 작성</button>
        </div>

        <div id="postModal" class="modal hidden fixed inset-0 z-50 overflow-y-auto bg-zinc-900/45 p-4 backdrop-blur-sm">
            <div class="modal-content mx-auto mt-12 w-full max-w-2xl rounded-3xl border border-white/70 bg-white p-6 shadow-2xl">
                <span class="close float-right cursor-pointer text-3xl font-semibold text-zinc-400 transition hover:text-zinc-700">&times;</span>
                <h2 class="mb-4 text-xl font-semibold text-zinc-800">게시글 작성</h2>
                <form id="postForm" class="space-y-3">
                    <div class="form-group">
                        <label for="postTitle" class="mb-1 block text-sm font-semibold text-zinc-600">제목</label>
                        <input type="text" id="postTitle" name="title" required class="w-full rounded-xl border border-zinc-200 px-4 py-3 text-sm outline-none focus:border-cyan-400">
                    </div>
                    <div class="form-group">
                        <label for="postContent" class="mb-1 block text-sm font-semibold text-zinc-600">내용</label>
                        <textarea id="postContent" name="content" rows="5" required class="w-full rounded-xl border border-zinc-200 px-4 py-3 text-sm outline-none focus:border-cyan-400"></textarea>
                    </div>
                    <button type="button" id="submitPostBtn" class="w-full rounded-xl bg-zinc-900 px-4 py-3 text-sm font-semibold text-white transition hover:bg-zinc-800">등록</button>
                </form>
            </div>
        </div>

        <div id="viewPostModal" class="modal hidden fixed inset-0 z-50 overflow-y-auto bg-zinc-900/45 p-4 backdrop-blur-sm">
            <div class="modal-content mx-auto mt-12 w-full max-w-3xl rounded-3xl border border-white/70 bg-white p-6 shadow-2xl">
                <span class="close-view float-right cursor-pointer text-3xl font-semibold text-zinc-400 transition hover:text-zinc-700">&times;</span>
                <h2 id="viewPostTitle" class="mb-4 text-2xl font-semibold text-zinc-800">제목</h2>
                <p id="viewPostContent" class="rounded-2xl border border-zinc-200 bg-zinc-50 p-4 text-sm leading-6 text-zinc-700">내용</p>
                <div class="post-meta mt-3 text-sm text-zinc-500">
                    <span id="viewPostDate">작성일</span> | <span id="viewPostWriter">작성자</span>
                </div>

                <div class="comment-section mt-5 rounded-2xl border border-zinc-200 bg-zinc-50 p-4">
                    <h3 class="mb-3 text-sm font-semibold text-zinc-700">댓글</h3>
                    <div id="commentList" class="space-y-2"></div>
                </div>

                <div class="comment-form mt-4">
                    <h3 class="mb-2 text-sm font-semibold text-zinc-700">댓글 작성</h3>
                    <form id="commentForm" class="space-y-3">
                        <div class="form-group">
                            <label for="commentContent" class="mb-1 block text-sm font-semibold text-zinc-600">내용</label>
                            <textarea id="commentContent" name="commentContent" rows="3" required class="w-full rounded-xl border border-zinc-200 px-4 py-3 text-sm outline-none focus:border-cyan-400"></textarea>
                        </div>
                        <button type="button" id="submitCommentBtn" class="w-full rounded-xl bg-zinc-900 px-4 py-3 text-sm font-semibold text-white transition hover:bg-zinc-800">댓글 작성</button>
                    </form>
                </div>

                <div class="modal-buttons mt-4 flex flex-wrap gap-2">
                    <button type="button" id="editPostBtn" class="modal-button rounded-xl bg-emerald-500 px-4 py-2 text-sm font-semibold text-white transition hover:bg-emerald-600">수정</button>
                    <button type="button" id="deletePostBtn" class="modal-button rounded-xl bg-rose-500 px-4 py-2 text-sm font-semibold text-white transition hover:bg-rose-600">삭제</button>
                </div>
            </div>
        </div>

        <div id="editPostModal" class="modal hidden fixed inset-0 z-50 overflow-y-auto bg-zinc-900/45 p-4 backdrop-blur-sm">
            <div class="modal-content mx-auto mt-12 w-full max-w-2xl rounded-3xl border border-white/70 bg-white p-6 shadow-2xl">
                <span class="close-edit float-right cursor-pointer text-3xl font-semibold text-zinc-400 transition hover:text-zinc-700">&times;</span>
                <h2 class="mb-4 text-xl font-semibold text-zinc-800">게시글 수정</h2>
                <form id="editPostForm" class="space-y-3">
                    <div class="form-group">
                        <label for="editPostTitle" class="mb-1 block text-sm font-semibold text-zinc-600">제목</label>
                        <input type="text" id="editPostTitle" name="title" required class="w-full rounded-xl border border-zinc-200 px-4 py-3 text-sm outline-none focus:border-cyan-400">
                    </div>
                    <div class="form-group">
                        <label for="editPostContent" class="mb-1 block text-sm font-semibold text-zinc-600">내용</label>
                        <textarea id="editPostContent" name="content" rows="5" required class="w-full rounded-xl border border-zinc-200 px-4 py-3 text-sm outline-none focus:border-cyan-400"></textarea>
                    </div>
                    <button type="button" id="saveEditPostBtn" class="w-full rounded-xl bg-zinc-900 px-4 py-3 text-sm font-semibold text-white transition hover:bg-zinc-800">저장</button>
                </form>
            </div>
        </div>
    </div>
    <script src="/javascripts/community.js"></script>
</body>
</html>
